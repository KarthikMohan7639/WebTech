<Window x:Class="TodoClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Todo List Client" Height="500" Width="600"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Shared Todo List" 
                   FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>
        
        <!-- Todo Items List -->
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" 
                CornerRadius="5" Padding="10">
            <ListView x:Name="TodoListView" ItemsSource="{Binding TodoItems}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Status" Width="80">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsCompleted}" 
                                              Checked="CheckBox_Checked"
                                              Unchecked="CheckBox_Unchecked"
                                              HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Description" Width="400">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Description}" 
                                               VerticalAlignment="Center"
                                               TextDecorations="{Binding IsCompleted, Converter={StaticResource BoolToStrikethroughConverter}}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="ID" Width="50">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Id}" 
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
        
        <!-- Add Item Section -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,20,0,0">
            <TextBox x:Name="NewItemTextBox" Width="400" Height="30" 
                     VerticalContentAlignment="Center"
                     KeyDown="NewItemTextBox_KeyDown"
                     Margin="0,0,10,0"/>
            <Button x:Name="AddButton" Content="Add Item" 
                    Width="100" Height="30"
                    Click="AddButton_Click"/>
        </StackPanel>
    </Grid>
    
    <Window.Resources>
        <!-- Converter for strikethrough text when completed -->
        <local:BoolToTextDecorationsConverter x:Key="BoolToStrikethroughConverter"/>
    </Window.Resources>
</Window>